/**
 * 배포할 때 채우는 값.
 *
 * 공개 저장소에는 비어 있다. 여기에 키를 넣지 않는다. 확장에 들어간 키는 설치한 사람이
 * 소스를 그대로 볼 수 있어 처음부터 비밀이 아니다. 키는 중계 Worker 의 환경 변수에만 둔다.
 * tools/check.py 의 반출 게이트가 키가 들어간 채 커밋되는 것을 막는다.
 */

/**
 * 체험용 중계 주소.
 *
 * 주소는 비밀이 아니므로 저장소에 둔다. 그래야 소스에서 빌드한 것과 스토어에 올린 것이
 * 같은 빌드가 되고, 쓰는 사람이 설정에서 방식을 골라 바꿀 수 있다. 감춰 봐야 확장을
 * 설치한 사람이 소스에서 그대로 읽는다. 남용은 중계 쪽 한도가 막는다.
 *
 * 비어 있으면 체험을 고를 수 없고 본인 키로만 동작한다.
 *
 * 이 저장소를 포크해 쓰는 사람은 자기 Worker 를 배포하고 그 주소로 바꾼다. 남의 주소를
 * 그대로 두면 그 사람의 예산을 쓰게 된다.
 *
 * 주소를 넣을 때 manifest.json 의 host_permissions 에도 같은 주소를 더한다.
 */
const NMT_RELAY_URL = 'https://mail-triage-relay.ejoongseok.workers.dev';
